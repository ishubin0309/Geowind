<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Climactif</title>
    <link rel="icon" type="image/png" href="{{ asset('favicon.png') }}?v=1">
    <link rel="stylesheet" href="{{ asset('vendor/bootstrap-3.3.7-dist/css/bootstrap.min.css') }}">
    <link rel="stylesheet" href="{{ asset('vendor/font-awesome-4.7.0/css/font-awesome.min.css') }}">
    <link rel="stylesheet" href="{{ asset('css/theme.css') }}?v=201711200000">
  </head>
  <body>
    {# <nav class="navbar navbar-default primary-navbar">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
            <a class="navbar-brand" href="{{ path('homepage') }}"><img src="" alt=""></a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
          <ul class="nav navbar-nav navbar-right">
            <li><a href="{{ path('homepage') }}">Accès pépinière</a></li>
            <li><a href="{{ path('cartographie') }}">Accès parcs éoliens</a></li>

          </ul>
        </div>
      </div>
    </nav> #}
    <!-- Navigation -->
   <a id="menu-toggle" href="#" class="btn btn-dark btn-lg toggle"><i class="fa fa-bars"></i></a>
    <nav id="sidebar-wrapper">
      <ul class="sidebar-nav">
        <a id="menu-close" href="#" class="btn btn-light btn-lg pull-right toggle"><i class="fa fa-times"></i></a>
        <li class="sidebar-brand">
          <a href="#missions" class="jumpto" onclick=$("#menu-close").click();>Climactif</a>
        </li>
        <li>
          <a href="#missions" class="jumpto" onclick=$("#menu-close").click();>Nos missions</a>
        </li>
        <li>
          <a href="{{ path('homepage') }}" onclick=$("#menu-close").click();>Accès pépinière</a>
        </li>
        <li>
          <a href="{{ path('cartographie') }}" onclick=$("#menu-close").click();>Accès parcs éoliens</a>
        </li>
      </ul>
    </nav>

    <!-- Header -->
    {# <header id="top" class="header">
      <div class="text-vertical-center">
      </div>
    </header> #}


    <section id="missions" class="col-xs-12 text-center">
      <img src="{{ asset('images/title.jpg') }}" style="width: inherit;max-width:1000px;" alt="Titre">
      <img src="{{ asset('images/mission.jpg') }}" style="width: inherit;max-width:1200px;" alt="Nos missions">
      {# <p id="font-size: x-large;color: #5C7046;">
      <strong>Climactif,</strong> des projets d'énergies renouvelables éthiques et esthétiques
      <br>
      <br>
      Les missions de nos ingénieurs, paysagistes, urbanistes, architectes
      <br>
      - proposer des sites adaptés aux projets d'énergies renouvelables
      <br>
      - concevoir des aménagements de haute qualité environnementale
      <br>
      <br>
      Notre conviction : neutralité carbone en 2050 !
      </p> #}
    </section>
    {# <section id="image2" class="col-xs-12">
      <img src="{{ asset('images/image2.jpg') }}" style="width: inherit;" alt="Image1">
      <img src="{{ asset('images/image3.jpg') }}" style="width: inherit;" alt="Image2">
    </section> #}
    <!-- Carousel
    ================================================== -->
    <div id="myCarousel" style="clear:both;" class="carousel slide" data-ride="carousel">
      <!-- Indicators -->
      <ol class="carousel-indicators">
        <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
        <li data-target="#myCarousel" data-slide-to="1"></li>
        <li data-target="#myCarousel" data-slide-to="2"></li>
      </ol>
      <div class="carousel-inner" role="listbox">
        <div class="item active">
          <img class="first-slide" src="{{ asset('images/image1.jpg') }}" alt="First slide">
        </div>
        <div class="item">
          <img class="second-slide" src="{{ asset('images/image2.jpg') }}" alt="Second slide">
        </div>
        <div class="item">
          <img class="third-slide" src="{{ asset('images/image3.jpg') }}" alt="Third slide">
        </div>
      </div>
      <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
      </a>
      <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
      </a>
    </div><!-- /.carousel -->


    <!-- Footer -->
    <footer>
      <div class="container">
        <div class="row">
          <div class="col-lg-10 col-lg-offset-1 text-center">
            
            <img src="{{ asset('images/logo.jpg') }}" alt="" style="height: 50px; margin-bottom: 10px;">
            <ul class="list-unstyled">
              <li><i class="fa fa-envelope-o fa-fw"></i> <a href="mailto:info@climactif.com">info@climactif.com</a>
              </li>
            </ul>

          </div>
        </div>
      </div>
      <a id="to-top" href="#top" class="btn btn-dark btn-lg"><i class="fa fa-chevron-up fa-fw fa-1x"></i></a>
    </footer>

    <script src="{{ asset('vendor/jquery/jquery-3.2.1.min.js') }}"></script>
    <script src="{{ asset('vendor/bootstrap-3.3.7-dist/js/bootstrap.min.js') }}"></script>


    <!-- Custom Theme JavaScript -->
    <script>
      // Closes the sidebar menu
      $("#menu-close").click(function (e) {
        e.preventDefault();
        $("#sidebar-wrapper").toggleClass("active");
      });
      // Opens the sidebar menu
      $("#menu-toggle").click(function (e) {
        e.preventDefault();
        $("#sidebar-wrapper").toggleClass("active");
      });

      $(function () {
        $('.jumpto').click(function () {
          if (location.pathname.replace(/^\//, '') == this.pathname.replace(/^\//, '') || location.hostname == this.hostname) {
            var target = $(this.hash);
            target = target.length ? target : $('[name=' + this.hash.slice(1) + ']');
            if (target.length) {
              $('html,body').animate({
                scrollTop: target.offset().top
              }, 1000);
              return false;
            }
          }
        });
      });

      //#to-top button appears after scrolling
      var fixed = false;
      $(document).scroll(function () {
        if ($(this).scrollTop() > 250) {
          if (!fixed) {
            fixed = true;
            // $('#to-top').css({position:'fixed', display:'block'});
            $('#to-top').show("slow", function () {
              $('#to-top').css({
                position: 'fixed',
                display: 'block'
              });
            });
          }
        } else {
          if (fixed) {
            fixed = false;
            $('#to-top').hide("slow", function () {
              $('#to-top').css({
                display: 'none'
              });
            });
          }
        }
      });

    </script>



  </body>

</html>